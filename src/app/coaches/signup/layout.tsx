import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Start Coaching | baisics',
};

export default function CoachSignupLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
